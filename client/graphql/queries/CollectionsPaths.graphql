query Collections(
  $filters: CollectionFiltersInput
  $pagination: PaginationArg
  $sort: [String]
  $publicationState: PublicationState
) {
  collections {
    data {
      id
      attributes {
        name
      }
    }
    meta {
      ...Pagination
    }
  }
}
